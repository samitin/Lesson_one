<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.samitin.lesson_one">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lesson_one">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Task1Activity"
            android:label="@string/main_menu_button_text_1" />
        <activity
            android:name=".Task2Activity"
            android:label="@string/main_menu_button_text_2"/>
        <activity
            android:name=".Task3Activity"
            android:label="@string/main_menu_button_text_3"/>

    </application>
</manifest>